---
- name: Test ping host all
  hosts: main_server
  #become: yes
  gather_facts: yes

  tasks:
  - name: COPY SRC to host
    include_role:
      name: create_user
      tasks_from: main
    vars:
      NEW_USER: '{{ item }}'
    loop: '{{user_list}}'
